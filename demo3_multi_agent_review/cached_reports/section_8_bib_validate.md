# Bibliography Validation Report

> **Sample output.** This report was produced by the `/validate-bib`
> skill against a specific working paper (Johnson & Taylor, *Mapping
> the Market for Ideas in Europe, 1450–1650*). The bib file name, key
> counts, and unused-entry list reflect that manuscript. Treat this
> file as a template for what a bibliography-check report looks like;
> running the same skill on your own project will produce a report
> keyed to your own `.bib` and `.tex` files.

Generated by the `/validate-bib` skill on 2026-06-09.
Manuscript-wide check (the `/validate-bib` skill operates across the
whole paper, not section-by-section).

---

## Summary

- **Bibliography file:** `Bibliography_draft.bib` (75 entries)
- **Unique citation keys used in manuscript:** 62
- **Missing entries** (cited but not in bib): **0** ✓
- **Unused entries** (in bib but never cited): **13**
- **Potential typos** in citation keys: **0** ✓
- **Malformed bib entries:** **0** ✓

Every `\cite{}` key in the manuscript resolves to a bib entry. The
paper is citation-clean.

---

## Unused bibliography entries

Present in `Bibliography_draft.bib` but never cited from `Paper/`.
Most are either references that got dropped during revision or
planned-but-not-used citations. Not critical — leaving them in the
bib does no harm — but consider cleaning before final submission.

| Key | Likely category |
|---|---|
| `Abramson2017_territorial` | dropped during revision |
| `Barro1991_growth` | planned-but-not-used (growth ref) |
| `Combes2008_economic_geography` | planned-but-not-used |
| `Couture2021_ecommerce` | planned-but-not-used (modern comparison) |
| `Fujita1999_spatial_economy` | planned-but-not-used |
| `Hamilton2016_diachronic` | could re-cite in §2 |
| `HandburyWeinstein2015_goods` | planned-but-not-used |
| `Hoberg2010_textual` | could re-cite in §2 |
| `Kelly2021_measuring` | could re-cite in §2 |
| `Mankiw1992_growth_empirics` | planned-but-not-used |
| `SalaIMartin1996_convergence` | planned-but-not-used |
| `Schiff2015_variety` | planned-but-not-used |
| `Wooldridge2010_panel` | already cited via `\citep` shortcut form |

The `Wooldridge2010_panel` flag is a false positive — it's cited as
`\citep[Ch.~10]{Wooldridge2010_panel}` which the regex extractor
strips down to the bare key, but the cross-reference catches it
correctly. Including for transparency.

---

## Citation style consistency (advisory)

- Author-year forms: `\citet{}` and `\citep{}` both used appropriately
  for in-line author cites and parenthetical cites.
- Multi-key cites: 11 instances of `\citet{Key1,Key2,...}` in the
  manuscript. Per the §8 proofreader finding H5, multi-key
  `\citet` can typeset awkwardly with natbib — consider whether to
  split or use `\citep` for grouped multi-cites.

---

## What's clean

1. No missing bib entries — manuscript compiles without `?` placeholders.
2. No typos in citation keys.
3. All 75 entries have required BibTeX fields populated.
4. Author and year fields well-formed throughout.
